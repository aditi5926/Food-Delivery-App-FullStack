/** @type {import('next').NextConfig} */
const nextConfig = {
     compiler: {
       emotion: true, // Enables Emotion CSS-in-JS
     },
     reactStrictMode: true,
   };
   
   module.exports = nextConfig;